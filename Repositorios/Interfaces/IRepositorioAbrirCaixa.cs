using CasaMendes;

namespace JoseMendes
{
    public interface IRepositorioAbrirCaixa<T> : IRepositorioBase<T>
    {
        //Task<bool> CriarTabela();
        //Task<bool> Insert<T>(T entity, bool enableIdentityInsert = false);
        //Task<bool> Update<T>(T entity);
        
        //Task<List<T>> SelectWithWhere<T>(string tableName, string whereClause, Dictionary<string, object> parameters = null);
        //Task<List<T>> SelectWithLike<T>(string tableName, string likeClause, Dictionary<string, object> parameters = null);
        
        //Task<bool> TabelaExiste();
    }
}